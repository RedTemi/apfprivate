import firebase from "firebase/app"
import "firebase/auth";
import "firebase/firestore";

const firebaseConfig = {
    //killed for the glory of the king.
  };
  // Initialize Firebase
export default firebase.initializeApp(firebaseConfig);
